{
  "name": "krmx-state",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "postinstall": "npm run install-all",
    "install-all": "npm --prefix state install && npm --prefix server install && npm --prefix client install",
    "dev": "npm-run-all --parallel dev:*",
    "dev:state": "npm run --prefix state dev",
    "dev:server": "npm run --prefix server dev",
    "dev:client": "npm run --prefix client dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "husky": "^4.3.8"
  }
}
